export default function NotFound() {
    return (
        <main className="not-found">
            <h1>Not Found</h1>
            <p>Unfortunetly, we could not found the requested page or resource.</p>
        </main>
    );
}